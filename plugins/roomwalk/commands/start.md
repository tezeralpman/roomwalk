---
description: Start a roomwalk — asks for the shop's site link, reads it, proposes a plan
---

Load the `roomwalk` skill and follow it from step 1.

Before anything, check that the Higgsfield tools (`generate_image`, `generate_video`)
are in this session. If they are not, run `/roomwalk:connect` first and stop — there is
nothing to build without them.

Then do exactly one thing: ask the user for the link to the shop's website. One line,
no other questions. Everything else — what they make, how many photographs they have,
their prices, their differentiator — comes from reading that site in step 2.

$ARGUMENTS
